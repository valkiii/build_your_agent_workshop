# Runtime dependencies for the Content Curator workshop project.
# Installed by setup.command / setup.bat into a local .venv.
beautifulsoup4>=4.12
ebooklib>=0.18
feedparser>=6.0
lxml>=5.0
markdown>=3.5
ollama>=0.3
requests>=2.31
streamlit>=1.30
trafilatura>=1.8

# Optional audio "podcast" output (narrator_agent.py)
kokoro-onnx>=0.4      # natural neural TTS, CPU (default engine)
piper-tts>=1.3        # tiny robotic TTS, fallback engine (config.TTS_ENGINE = "piper")
numpy>=1.24
imageio-ffmpeg>=0.5   # portable ffmpeg for MP3 encoding (falls back to WAV if absent)
